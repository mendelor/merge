apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: test1
  labels:
    run: nginx
  creationTimestamp: null
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
